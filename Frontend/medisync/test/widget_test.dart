// Smoke test — verifies that MediSyncApp renders without throwing.
// Full integration tests will be added per-feature.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
